import { Box } from '@chakra-ui/react';
import FormComponent from './FormComponent';

const BodyComponent = () => {
  return (
    <Box mt={20}>
      <FormComponent />
    </Box>
  );
};

export default BodyComponent;
